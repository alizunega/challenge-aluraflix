import { styled } from "@mui/material"

const StyledMain = styled("main")({ 
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    height: "auto"
})

const Main = () => {
    return <>
    </>
}

export default Main